#include <stdlib.h>

#include "mic.h"
#include "graph.h"
#include "graph_internal.h"
#include "union_find.h"

//return the minimum weight edge coming out of each vertex
Edge* min_edges(Graph g){
    int num_nodes = get_num_nodes(g);

    for(int i = 0; i < num_nodes; i++){

    }
    return NULL;
}

Graph star_contract(Graph g){
    return NULL;
}

Graph find_MST(Graph g){
    return NULL;
}
